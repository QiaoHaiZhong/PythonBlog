第一次使用本模块，如果不知道如何使用，可以看看下面的教材。


如何上传代码到服务器

如何生成需要安装的环境模块
pip freeze > requirements.txt

当需要创建这个虚拟环境的完全副本，可以创建一个新的虚拟环境，并在其上运行以下命令：
pip install -r requirements.txt

第一次使用的时候，需要修改数据库ip地址，端口号，用户名和密码，还需要新建一个数据库，默认数据库名是pyblog
这个库名你可以随便起，如果改库名，也要在配置文件里面做相应的修改，配置文件名：config.py
